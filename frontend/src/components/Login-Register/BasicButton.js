import * as React from 'react';
import Button from '@mui/material/Button';

export default function BasicButton() {
  return (
      <Button sx={{ width: "430px", height: "35px" }} variant="contained" >Contained</Button>
  );
}